# البدء السريع

## التثبيت

نزّل أحدث مثبّت من [صفحة التنزيل](/ar/download) أو [GitHub Releases](https://github.com/wiseSpace-Desktop/wiseSpace/releases).

### macOS

| الشريحة | الملف |
|---------|-------|
| Apple Silicon (M1 / M2 / M3 / M4) | `wiseSpace_x.x.x_aarch64.dmg` |
| Intel | `wiseSpace_x.x.x_x64.dmg` |

1. افتح `.dmg` واسحب **wiseSpace** إلى مجلد **Applications**.
2. أطلق wiseSpace. إذا حظرت macOS التطبيق، اذهب إلى **إعدادات النظام ← الخصوصية والأمان** وانقر **فتح على أي حال**.

::: warning macOS: "التطبيق تالف" أو "لا يمكن التحقق من المطوّر"
إذا رأيت أياً من هذه الرسائل، افتح Terminal ونفّذ:

```bash
xattr -c /Applications/wiseSpace.app
```

ثم أعد تشغيل التطبيق.
:::

### Windows

| البنية | الملف |
|--------|-------|
| x64 (معظم الأجهزة) | `wiseSpace_x.x.x_x64-setup.exe` |
| ARM64 | `wiseSpace_x.x.x_arm64-setup.exe` |

شغّل المثبّت واتبع المعالج.

### Linux

```bash
# Debian / Ubuntu
sudo dpkg -i wiseSpace_x.x.x_amd64.deb

# Fedora / openSUSE
sudo rpm -i wiseSpace_x.x.x_x86_64.rpm

# AppImage
chmod +x wiseSpace_x.x.x_amd64.AppImage
./wiseSpace_x.x.x_amd64.AppImage
```

---

## الإعداد الأولي

### 1. فتح الإعدادات

أطلق wiseSpace وانقر على **أيقونة الترس** في أسفل الشريط الجانبي، أو اضغط <kbd>Cmd/Ctrl</kbd>+<kbd>,</kbd>.

### 2. إضافة مزوّد

انتقل إلى **الإعدادات ← المزوّدون** وانقر على زر **+**.

1. أدخل اسماً للعرض (مثلاً *OpenAI*).
2. اختر نوع المزوّد (OpenAI أو Anthropic أو Google Gemini، إلخ).
3. الصق مفتاح API الخاص بك.
4. أكّد **Base URL** — الطرف الرسمي مُملوء مسبقاً للأنواع المدمجة.

::: tip
يمكنك إضافة أي عدد من المزوّدين. كل مزوّد يدير مفاتيح API ونماذجه بشكل مستقل.
:::

### 3. جلب النماذج

انقر **جلب النماذج** للحصول على قائمة النماذج المتاحة من API المزوّد. يمكنك أيضاً إضافة معرّفات النماذج يدوياً إذا لزم.

### 4. ضبط النموذج الافتراضي

اذهب إلى **الإعدادات ← النموذج الافتراضي** واختر المزوّد والنموذج الذي تستخدمه المحادثات الجديدة افتراضياً.

---

## محادثتك الأولى

1. انقر **محادثة جديدة** في الشريط الجانبي (أو اضغط <kbd>Cmd/Ctrl</kbd>+<kbd>N</kbd>).
2. اختر نموذجاً من محدد النماذج في أعلى المحادثة.
3. اكتب رسالة واضغط <kbd>Enter</kbd>.
4. يبثّ wiseSpace الاستجابة في الوقت الفعلي. النماذج التي تدعم كتل التفكير (مثل Claude وDeepSeek R1) تعرض عملية الاستدلال في قسم قابل للطي.

---

## اختصارات لوحة المفاتيح

| الاختصار | الإجراء |
|----------|---------|
| <kbd>Cmd/Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>A</kbd> | إظهار / إخفاء النافذة الحالية |
| <kbd>Cmd/Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>Alt</kbd>+<kbd>A</kbd> | إظهار / إخفاء جميع النوافذ |
| <kbd>Cmd/Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>W</kbd> | إغلاق النافذة |
| <kbd>Cmd/Ctrl</kbd>+<kbd>N</kbd> | محادثة جديدة |
| <kbd>Cmd/Ctrl</kbd>+<kbd>,</kbd> | فتح الإعدادات |
| <kbd>Cmd/Ctrl</kbd>+<kbd>K</kbd> | لوحة الأوامر |
| <kbd>Cmd/Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>G</kbd> | تبديل بوابة API |

---

## البيانات والنسخ الاحتياطي

### دلائل البيانات

| المسار | المحتوى |
|--------|---------|
| `~/.wisespace/` | حالة التطبيق — قاعدة البيانات ومفاتيح التشفير وقاعدة البيانات المتجهة وشهادات SSL |
| `~/Documents/wisespace/` | ملفات المستخدم — الصور والمستندات والنسخ الاحتياطية |

---

## الخطوات التالية

- [إعداد المزوّدين](./providers) — إضافة وإدارة مزوّدي AI
- [خوادم MCP](./mcp) — توصيل أدوات خارجية لتوسيع قدرات AI
- [بوابة API](./gateway) — كشف مزوّديك كخادم API محلي
