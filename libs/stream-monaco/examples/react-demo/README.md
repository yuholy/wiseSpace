# React Demo (React + Vite)

A minimal React example for `stream-monaco`.

- Creates a Monaco editor from React
- Switches language and theme without remounting
- Streams content with `appendCode`

## Quick start

```bash
pnpm i
pnpm -C examples/react-demo dev
```

Then open http://localhost:5173
