[package]
name = "wisespace-migration"
version = "0.1.1"
edition = "2021"

[dependencies]
sea-orm-migration = { version = "1", features = ["sqlx-sqlite", "runtime-tokio-native-tls"] }
async-trait = "0.1"
tokio = { workspace = true }
