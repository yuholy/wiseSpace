# wiseSpace Agent 强化计划

## 1. 目标

本计划用于指�?wiseSpace 后续 Agent 能力建设，目标不是简单追逐“更像聊天机器人”，而是�?wiseSpace 强化为一个：

- 本地优先�?Agent 工作�?
- 可控、可追踪、可恢复的任务执行系�?
- 统一管理聊天、任务、工具、工作空间、MCP 与记忆能力的个人控制�?

本计划默认以 `wiseSpace Local` 为核心能力建设对象，外部 Agent 生态作为补充扩展层，而不是主路径替代物�?

## 2. 当前判断

基于现有实现，wiseSpace 已经具备以下基础�?

- 本地工作空间执行能力
- 任务中心与运行记�?
- 工具调用事件流与时间�?
- 权限审批、补问、恢复、中断等交互能力
- MCP 可扩展能�?
- 会话、任务、数据的本地闭环

当前短板主要集中在：

- 长链路任务的稳定性与收敛能力不足
- 任务状态同步与界面反馈仍不够一�?
- Agent 结果质量更多依赖接入模型，而非产品层策略增�?
- 任务拆解、恢复、回放、重试策略还偏基础
- 对代码型任务、研究型任务、自动化任务的能力分层还不够清晰

## 3. 核心强化方向

### 3.1 任务执行稳定�?

目标：让 Agent 能稳定地“跑完、看懂、接着跑”�?

重点事项�?

- 统一 run、session、event、step 的状态语�?
- 修复列表、详情、会话内状态不同步问题
- 强化异常退出、窗口关闭、应用重启后的恢复逻辑
- 明确 `completed / failed / cancelled / interrupted / waiting_*` 的状态边�?
- 为任务中心补充状态一致性自检

验收标准�?

- 同一任务在任务列表、详情页、会话页状态必须一�?
- 重启后未完成任务可以明确标记并恢复或重跑
- 所有异常结束都能留下可解释的错误摘�?

### 3.2 Agent 交互闭环

目标：让用户始终知道 Agent 在做什么，何时需要介入�?

重点事项�?

- 优化任务中心与会话中的事件展�?
- 标准化工具调用、权限请求、补问、结果完成的可视反馈
- 给关键任务增加阶段性进度摘�?
- 为运行中任务补充更清晰的“当前动作”与“最后动作”展�?
- 为失败任务补充建议动作，如重跑、恢复、查看上下文、打开工作空间

验收标准�?

- 用户在不看日志的前提下，能理解任务当前阶段和阻塞原因
- 权限请求与补问不会静默丢�?
- 失败任务具备最小可用的下一步指�?

### 3.3 工具与权限系�?

目标：让 Agent 更能做事，同时不失控�?

重点事项�?

- 补强工具风险分类与权限策�?
- 区分读、写、执行、联网等高频操作类型
- 建立“会话级允许”“任务级允许”“单次允许”边�?
- 完善权限批准后的事件回写与状态恢�?
- �?MCP 工具统一权限呈现

验收标准�?

- 工具行为与权限弹窗的风险表述一�?
- 批准后的执行路径可追�?
- 任务在等待权限和权限处理后状态切换正�?

### 3.4 工作空间与代码型任务能力

目标：把 wiseSpace 从“能跑任务”提升到“更适合真实本地项目工作”�?

重点事项�?

- 补强工作空间初始化与目录创建逻辑
- 为代码任务建立更稳定的文件修改、重试、回滚提示链�?
- 增强任务与工作空间路径的映射展示
- 增加对“项目分�?/ 修改 / 验证 / 总结”这类代码任务模式的支持
- 为任务结果补充更明确的文件影响面说明

验收标准�?

- 新建任务不因默认工作空间缺失而失�?
- 代码任务结束后，能清楚看到改动目标与运行结果
- 用户可以从任务中心快速跳转会话和工作空间

### 3.5 任务恢复、重跑与自动�?

目标：让任务具备更强的持续工作能力�?

重点事项�?

- 明确“恢复”和“重跑”的产品语义差异
- �?interrupted 任务增加可恢复上下文校验
- �?completed / failed 任务增加带上下文的重跑策�?
- 评估�?automation/heartbeat 的衔接方�?
- 补充任务模板能力，沉淀常用任务类型

验收标准�?

- 恢复不会错误复用不兼容上下文
- 重跑能够继承必要参数但不继承错误状�?
- 任务模板可覆盖高频使用场�?

### 3.6 结果质量增强

目标：减少“只是把模型输出展示出来”，增强产品层的任务完成质量�?

重点事项�?

- 增加任务前置上下文整�?
- 为研究、代码、自动化等任务建立不同提示策�?
- 增加失败重试与错误分�?
- 评估引入更清晰的计划步骤、阶段总结、最终总结结构
- 强化首轮标题、摘要、影响范围生成质�?

验收标准�?

- 相同模型下，任务完成质量与可读性明显高于普通聊�?
- 失败原因可归类，不再大量表现为“无结果结束�?

## 4. 分阶段执行建�?

### Phase 1：稳定性优�?

- 修复状态同步问�?
- 修复任务中心与会话页的交互不一�?
- 统一任务生命周期状�?
- 强化任务启动、失败回滚、恢复逻辑

### Phase 2：交互与可视�?

- 优化任务中心页面
- 优化任务时间线和待处理卡�?
- 强化运行中、等待中、失败态的用户反馈
- 统一会话页与任务页的任务状态呈�?

### Phase 3：工具与权限体系

- 统一权限策略与风险展�?
- 接�?MCP 工具权限体验
- 强化工具调用结果可追踪�?

### Phase 4：代码型任务与自动化

- 强化工作空间、文件影响面、验证反�?
- 增强重跑、恢复、模板化
- 与自动化能力进行更稳定的衔接

## 5. 提交日志制度

从本计划生效起，仓库内每一次代码提交都必须附带提交日志。提交日志不是可选说明，而是提交的一部分�?

### 5.1 强制要求

每次提交必须包含以下内容�?

- 修改内容
- 修改目的
- 影响范围
- 风险与兼容性说�?
- 验证方式

如果某项确实不适用，必须明确写 `无`，不能省略�?

### 5.2 建议格式

推荐将提交信息分为标题和正文两部分：

标题�?

```text
type(scope): 简短说�?
```

正文模板�?

```text
修改内容:
- ...

修改目的:
- ...

影响范围:
- ...

风险与兼容�?
- ...

验证方式:
- ...
```

### 5.3 示例

```text
fix(task-center): 修复任务列表与详情状态不同步

修改内容:
- 调整任务中心 store 的刷新逻辑
- 在刷新列表后主动同步当前选中任务详情

修改目的:
- 解决左侧已完成但右侧仍显�?running 的问�?

影响范围:
- 任务中心列表
- 任务详情面板
- 任务状态刷新逻辑

风险与兼容�?
- 可能增加详情接口调用次数
- 不涉及数据库结构变更

验证方式:
- pnpm typecheck
- 手动创建任务并观察完成后左右状态是否一�?
```

## 6. 执行约束

- 后续涉及 Agent 能力增强的改动，应优先对照本计划归类
- 未经说明，不应提交只改表现、不说明原因�?Agent 相关修改
- Agent 相关提交必须补充验证方式，避免只�?UI 不验状态链�?- 如遇计划外能力扩展，应先更新本文件再推进实现

## 7. Related Issue Templates

To turn this plan into directly executable work items, reuse:

- [wiseSpace Local Agent Issue Templates](./local-agent-issue-templates.md)

This companion document is the preferred place to keep implementation-ready
issue breakdowns for the current local-agent track.


